import { environment } from '../../../../environments/environment';

export const AUTH_BASE_URL: string = `${environment.API_URL}/auth`;
export const USER_BASE_URL: string = `${environment.API_URL}/users`;
export const ACCOUNT_BASE_URL: string = `${environment.API_URL}/account`;
export const AD_BASE_URL = `${environment.API_URL}/ads`;
export const MEETING_BASE_URL = `${environment.API_URL}/meetings`;
export const PAYMENT_BASE_URL = `${environment.API_URL}/payment`;
